# Example — copy this to .env and fill in real values.
# These are SERVER-SIDE only (no VITE_ prefix = never bundled into the browser).
# For production: set these in Vercel Dashboard → Project → Settings → Environment Variables.
TMDB_API_KEY=your_tmdb_api_key_here
OMDB_API_KEY=your_omdb_api_key_here
